# Plan: Find Telegram Connection Error, Build EXE, Create Desktop Shortcut

## Project Overview
- **Name**: Furaya Telegram Promo Engine v5.5 Enterprise
- **Purpose**: Multi-account Telegram promotion bot with GUI for automated messaging
- **Tech Stack**: PyQt5, pytdbot (login/auth), Telethon (messaging), PyInstaller

## Deep Study Summary

### Project Structure
```
telegram-message-sender/
├── main.py                    # Entry point (qasync + PyQt5)
├── requirements.txt           # telethon, pyqt5, qasync, python-dotenv, Pillow
├── FurayaPromoEngine.spec    # PyInstaller spec file
├── installer.ps1           # Windows installer with shortcut creation
├── tdjson.dll            # Telegram TDLib native DLL
├── core/
│   ├── tdlib_engine.py       # pytdbot client for login/auth - handles connection states
│   ├── account_manager.py # Multi-account management
│   ├── account.py       # Account data model
│   ├── promo_pipeline.py # 3-phase autonomous pipeline
│   ├── promotion_engine.py # Telethon backend for messaging
│   └── ... (other engines)
├── gui/
│   ├── main_window.py
│   ├── accounts_tab.py
│   └── ... (UI components)
└── data/               # Runtime data (accounts.json, state.json, etc.)
```

### Connection Error Locations Found

#### 1. `core/tdlib_engine.py` (Primary Connection Error Handling)
- **Lines 317-321**: Connection state handling during login
  - `"connectingtoproxy"`: Connecting via proxy
  - `"connecting"`: General connection
  - `"waitingfornetwork"`: No network connection - CRITICAL ERROR state

- **Lines 282-300**: client.start() error handling with retry logic
- **Line 134**: Placeholder API credentials check
- **Line 184-189**: Phone number rejection errors
- **Line 204-209**: OTP rejection
- **Line 234-239**: 2FA rejection

#### 2. `core/account_manager.py`
- **Line 147**: Login exception handling
- **Line 159-160**: Failed login reason logging

#### 3. Connection-Related Status States
- `connected` - Successfully connected
- `connecting` - In progress
- `failed` - Connection failed  
- `pending` - Not yet tried
- `disconnected` - Logged out

## Tasks to Execute

### Task 1: Identify and Analyze Connection Errors (Priority: HIGH)
- [ ] Review connection error handling in tdlib_engine.py:302-325
- [ ] Check for common failure scenarios:
  - Invalid API credentials (placeholder check at line 133-135)
  - Network connectivity issues
  - Wrong database encryption key
  - OTP/2FA timeout
- [ ] Verify tdjson.dll is in correct location
- [ ] Test connection with dummy account

### Task 2: Fix Connection Issues (Priority: HIGH)
- [ ] Ensure proper error messages are displayed to user
- [ ] Add retry logic for transient failures
- [ ] Fix any identified bugs in error handling

### Task 3: Build EXE File (Priority: HIGH)
- [ ] Run PyInstaller with the spec file:
  ```
  pyinstaller FurayaPromoEngine.spec
  ```
- [ ] Output: `dist/FurayaPromoEngine_v5.5_Enterprise.exe`
- [ ] Verify exe runs standalone

### Task 4: Create Desktop Shortcut (Priority: HIGH)
- [ ] Option A: Run installer.ps1 (creates shortcut in C:\FurayaPromoEngine)
- [ ] Option B: Manual shortcut creation
- [ ] Verify shortcut works

## Potential Connection Errors to Fix
1. **Network timeout** - Increase timeout if needed
2. **Wrong encryption key** - Already has retry logic, but verify
3. **Missing DLL** - Ensure tdjson.dll is distributed
4. **Invalid API keys** - Better error message

## Build Output Location
- EXE: `dist/FurayaPromoEngine_v5.5_Enterprise.exe`
- Installer expects: `C:\FurayaPromoEngine\`